# The users the OTLP receivers accept, as one htpasswd line. Copy this file to
# tally-otlp-auth.env and paste the tally:$2y$12$... line that
# `htpasswd -nBC 12 tally` prints after the =, unquoted
# (docs/how-to/observability/publish-metrics-over-otlp.md).
htpasswd=
