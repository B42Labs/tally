# What VictoriaMetrics takes as -deleteAuthKey, the key its delete_series
# endpoint requires. Copy this file to tally-vm-admin.env and generate the value
# with `openssl rand -hex 32`.
delete-auth-key=
